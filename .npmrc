; Fail `pnpm install` (instead of just warning) when Node doesn't satisfy the "engines" field, so
; an unsupported Node is caught at install with a clear message rather than crashing later in the
; build with an obscure undici error.
engine-strict=true
